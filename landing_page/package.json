{
  "name": "landing_page",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-router-dom": "^6.14.2",
    "sanitize-html": "^2.12.1",
    "web-vitals": "^2.1.4"
  },
  "devDependencies": {
    "esbuild": "^0.28.0",
    "esbuild-sass-plugin": "^3.7.0",
    "sass": "^1.99.0"
  },
  "scripts": {
    "build": "mkdir -p build && cp -r public/ build/ && cp src/assets/orcid.svg build/ && node build.js"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
